<%= form_with(model: coupon, local: true) do |form| %>
  <% if coupon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coupon.errors.count, "error") %> prohibited this coupon from being saved:</h2>

      <ul>
      <% coupon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_field :code, id: :coupon_code %>
  </div>

  <div class="field">
    <%= form.label :discount %>
    <%= form.number_field :discount, id: :coupon_discount %>
  </div>

  <div class="field">
    <%= form.label :expire_date %>
    <%= form.datetime_select :expire_date, id: :coupon_expire_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
